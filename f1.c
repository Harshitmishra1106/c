#include<stdio.h>
#include<math.h>
int power(int ,int );
int main()
{
    int a,b,c;
    printf("enter a and b to get a raised to the power b");
    scanf("%d%d",&a,&b);
    c= power(int ,int );
    printf("%d is the result",c);
    return 0;
}
int power(int ,int )
{
    int m,n,x;
    x=pow(m,n);
    return x;
}
